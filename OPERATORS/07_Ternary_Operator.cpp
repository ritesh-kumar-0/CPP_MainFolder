//Ternary Operator is a short hand method of writing an if-else statement in a single line.

#include<iostream>
using namespace std;

int main(){
    int num = 10;
    (num % 2 == 0)? cout<<"Even": cout<<"Odd";

    return 0;
}